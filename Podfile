platform :ios, '12.0'

target 'cryptolists' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for cryptolists
  pod 'SwiftyJSON', '~> 4.0'
  pod 'CollapsibleTableSectionViewController', '~> 2.0.1'
  pod 'Kingfisher', '~> 6.0'
  pod 'SVGKit'

end
